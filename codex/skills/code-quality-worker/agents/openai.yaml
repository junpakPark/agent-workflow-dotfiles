interface:
  display_name: "Code Quality Worker"
  short_description: "Emit code-quality findings on a change surface"
  default_prompt: "Emit F-NNN + severity code-quality findings for the given change surface only. Do not modify code, do not decide a result, do not triage."
policy:
  allow_implicit_invocation: true
